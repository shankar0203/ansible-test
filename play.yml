---
 - name : install and configure the tomcat server
   hosts: all
   gather_facts: false
   become: true
   tasks:
    - name: update the machine
      yum: 
       - name: " * "
         state: latest
      
    - name: install java
      yum:
       - name: java-1.8.0
         state: latest
        
   
